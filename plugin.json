{
  "id": "meshy",
  "kind": "plugin",
  "version": "1.0.13",
  "label": "Meshy",
  "description": "Meshy AI API — Discover free community models first, then text/image-to-3D, remesh/retexture/convert/resize, auto-rig, animate. Import into Blender or UEFN Content Browser.",
  "min_app_version": "1.0.0",
  "default_enabled": false,
  "secret_keys": [
    "meshy_api_key"
  ],
  "contributes": {
    "settings.tabs": [
      {
        "id": "Meshy",
        "label": "Meshy",
        "icon": "assets/icon.png",
        "ui": "sections"
      }
    ],
    "settings.sections": [
      {
        "tab": "Meshy",
        "id": "account",
        "title": "Meshy",
        "description": "Paste your Meshy API key (from meshy.ai → Settings → API). Stored encrypted on this device. Powers meshy_* MCP tools for generate, remesh/retexture/convert, rig, animate, and import to Blender/UEFN.",
        "order": 10,
        "properties": [
          {
            "id": "meshy_api_key",
            "type": "secret",
            "label": "API key",
            "placeholder": "msy_…",
            "testable": true,
            "description": "Get a key at https://www.meshy.ai/settings/api — required for meshy_* tools (Pro+). Use Test to verify the key against Meshy."
          }
        ]
      }
    ],
    "walkthrough": {
      "id": "meshy",
      "title": "Meshy setup",
      "auto_start": "first_enable",
      "settings_tab": "Meshy",
      "steps": [
        {
          "target": "settings.tab.meshy",
          "title": "Paste your API key",
          "body": "Create a key at meshy.ai → Settings → API (msy_…), paste it here, then hit Test. Tools appear as meshy_* once this plugin is enabled. Do not run npx skills add meshy-dev/meshy-3d-agent — this plugin already ships the skill + MCP tools.",
          "advance": "next",
          "mode": "rect"
        },
        {
          "target": "settings.tab.meshy",
          "title": "Generate, rig, import",
          "body": "Agent checks Meshy Discover for FREE community models first (links in chat). Paid generate always asks via a panel question (ducky_ask_user) before confirm_spend. Land with meshy_import_to_uefn or meshy_import_to_blender. Characters: meshy_rig / animate → Blender skeletal_export.",
          "advance": "next"
        }
      ]
    },
    "agent.tools": {
      "category": "meshy",
      "intent_pattern": "\\b(meshy|meshy\\.ai|text[- ]?to[- ]?3d|image[- ]?to[- ]?3d|rig(?:ging)?|animat(?:e|ion)s?|discover|community)\\b",
      "plan_tools": [
        "meshy_balance",
        "meshy_status",
        "meshy_discover_search",
        "meshy_discover_get",
        "meshy_list_animations"
      ]
    }
  },
  "backend": {
    "entry": "backend",
    "register": "register"
  }
}
